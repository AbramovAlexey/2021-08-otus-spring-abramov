spring:
  datasource:
    url: jdbc:h2:mem:testdb
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: validate
    #show-sql: true
  liquibase:
    change-log: classpath:db/changelog/liquibase-changelog.xml